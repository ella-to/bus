version: "3.5"

services:
  bus:
    image: ellato/bus:latest
    # build: .

    # environment:

    ports:
      - "2021:2021"

    # make sure to use the right network
    networks:
      - bus

    volumes:
      - ./storage:/storage

networks:
  bus:
    name: bus_net
    driver: bridge